<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= if content_for?(:title) then yield(:title) + ' | ' end %>
      Fail to Fuel
    </title>
    <%= if content_for?(:head) then yield(:head) end %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= include_gon %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_pack_tag :application %>
    <%= render "layouts/favicon" %>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- reCAPTCHA -->
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>

    <!-- favicons -->

  </head>

  <body>
    <main>

      <header class="container text-center my-5">
        <%= image_tag "Logo.png", style: "width: 250px" %><br>
      </header>

      <section class="red-stripe p-5 mb-5">
        <h1 class="text-center narrow big">
          It is impossible to succeed without failing.
        </h1>
      </section>

      <section class="container">
        <h3 class="mb-5 text-center bold color-red">
          Learn how to weaponize good failures,
          <br>prevent catastrophic failure, and guarantee success.
        </h3>

        <div class="text-center floating-box p-5">
          <%= render partial: "layouts/coming_soon_form" %>
        </div>
      </section>

      <section class="red-stripe py-5 my-5">
        <h2 class="text-center">Participate in Failure Research</h2>
        <div class="floating-box p-5 my-4 color-black text-center">
          <%= render partial: "layouts/participant_signup" %>
        </div>
      </section>


      <footer class="container text-center my-5">
        <%= image_tag "Logo.png", style: "width: 250px; margin-bottom: 2rem" %><br>
        <% if current_user && current_user.admin %>
          <%= link_to "Home Page", home_index_path %>
        <% else %>
          <%= link_to "Admin Login", new_user_session_path %>
        <% end %>
      </footer>

    </main>
  </body>
</html>
